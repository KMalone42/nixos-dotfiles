[Seat:*]
# Text-based greetd configuration
# See https://github.com/lucasmkr/greetd

greeter = greetd-agreety

# Optional: terminal configuration
# terminal = ${pkgs.foot}/bin/foot
# terminal_args = -c ${pkgs.foot}/config

# Optional: use a TUI greeter with i3
# greeter = greetd-tui-i3

# Optional: background image
# bg_image = ${"${wallpapers_path}"}
